name: swepy_env
channels:
  - conda-forge
  - defaults
dependencies:
  # core python
  - pytest-cov
  - codecov
  - matplotlib
  - python=3.6
  - pytest
  - pandas
  - jupyter
  - ipykernel
  - requests
  - tqdm
  - scipy
  # geo libraries
  - pynco
  - gdal
  - scikit-learn
  - netCDF4
  - swepy
  - mapboxgl
  - jenkspy
  - zarr
  # aws libraries 
  - boto3
  - s3fs
  - xarray
  - fsspec

    
