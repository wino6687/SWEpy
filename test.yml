name: swepy_env
channels:
  - conda-forge
  - defaults
dependencies:
  # core python
  - pytest-cov
  - codecov
  - matplotlib
  - python=3.6
  - pytest
  - pandas==0.25.3
  - requests
  - tqdm
  # geo libraries
  - pynco
  - gdal
  - scikit-image
  - affine
  - netCDF4 
  - xarray
  - fsspec
  - mapboxgl
  - jenkspy
  - zarr